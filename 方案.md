# FCL → Miuix UI 迁移工程方案 — Kimi Code 集群模式

## 角色定位
你是 FCL（Fold Craft Launcher）Android 启动器的 UI 迁移架构师。任务：将 FCL 的原生 Android XML + Material Design UI 迁移到 Miuix（基于 Compose Multiplatform 的 HyperOS 风格组件库），保证所有功能无损、交互行为等价、体验一致或提升。

---

## 已核实的工程基线（事实层，勿再猜测）

以下数据已从当前仓库实测，方案中的命令与路径以此为准：

- **构建栈**：AGP `8.13.2`，Kotlin `2.3.20`，compileSdk `35`，minSdk `26`，targetSdk `34`，Java 17
- **现状**：`FCL` 模块**未启用 Compose**，仅开启 `viewBinding`；依赖由 `gradle/libs.versions.toml`（Version Catalog）统一管理，新增依赖必须走 Catalog，禁止在模块 build 文件里写硬编码坐标
- **模块结构**：`FCL`（App/UI）、`FCLCore`、`FCLibrary`、`FCLauncher`、`Terracotta`、`LWJGL-Pojav` 等；UI 迁移只动 `FCL` 模块
- **UI 规模**：`FCL/src/main/res/layout/` 共 **142 个 XML**，其中 `dialog_*` 约 40+ 个（对话框是迁移大户，需专门策略）；`AndroidManifest.xml` 声明 **9 个 Activity**（Splash/Main/Web/Controller/Shell/JVM/JVMCrash/Import 等）、仅 2 个 Fragment——UI 主体在 Activity + 自定义 View，而非 Fragment
- **图片加载**：项目已用 **Glide 4.16**（不是 Coil）。Compose 侧优先用 Glide 官方 Compose 集成；引入 Coil 属于新增依赖，须在阶段二做显式决策
- **持久化**：`androidx.datastore` 已是现有依赖，设置迁移可直接用，无需"可选升级"
- **构建验证命令**：`./gradlew :FCL:assembleDebug`（另有 `fordebug` buildType 带 `.debug` 后缀；CI 见 `.github/workflows/build.yml`）

### Miuix 依赖的真实坐标（已在 Maven Central 核实）

原方案写的 `miuix-ui:0.9.0` **不存在**。正确坐标（`top.yukonga.miuix.kmp` 组）：

```toml
# gradle/libs.versions.toml
[versions]
miuix = "0.8.8"   # 以执行时的最新 release 为准，执行前必须重新核实

[libraries]
miuix            = { module = "top.yukonga.miuix.kmp:miuix", version.ref = "miuix" }
miuix-icons      = { module = "top.yukonga.miuix.kmp:miuix-icons", version.ref = "miuix" }
miuix-preference = { module = "top.yukonga.miuix.kmp:miuix-preference", version.ref = "miuix" }
miuix-blur       = { module = "top.yukonga.miuix.kmp:miuix-blur", version.ref = "miuix" }
miuix-squircle   = { module = "top.yukonga.miuix.kmp:miuix-squircle", version.ref = "miuix" }
```

核实命令：`curl -s https://repo1.maven.org/maven2/top/yukonga/miuix/kmp/miuix/maven-metadata.xml | grep -E "latest|release"`

### Compose 启用方式（Kotlin 2.x 正确姿势）

原方案的 `composeOptions { kotlinCompilerExtensionVersion = ... }` 是 Kotlin 1.x 时代的写法，**已废弃**。Kotlin 2.3.20 必须用 Compose Compiler Gradle 插件：

```kotlin
// FCL/build.gradle.kts
plugins {
    alias(libs.plugins.compose.compiler)   // org.jetbrains.kotlin.plugin.compose，版本跟随 Kotlin
}
android {
    buildFeatures {
        compose = true                     // viewBinding 在迁移期保留，全部迁完后才移除
    }
}
```

### 组件名以 Miuix 源码为准

原方案中的 `MiuixText` / `MiuixCard` / `MiuixSwipeToDismiss` / `MiuixDialog` 等组件名**是推测，不保证存在**。阶段一必须产出一份《原生控件 → Miuix 组件映射表》，逐项对照 Miuix 实际源码/文档核实（如 `Text`、`Card`、`BasicComponent`、`SuperArrow`、`SuperSwitch` 等真实命名），后续所有迁移步骤只许引用表内已核实的组件。

---

## 集群协作约定（本工程为多 Agent 并行执行）

1. **共享产物落盘**：所有清单/映射表/报告写入 `docs/migration/` 目录作为文件（如 `docs/migration/ui-inventory.md`），供后续 Agent 直接读取，不依赖口头传递
2. **分支模型**：主分支 `feature/miuix-migration`；每个页面迁移开子分支 `feature/miuix-<页面名>`，验收通过后合回
3. **冲突热点串行化**：以下文件任一时刻只允许一个 Agent 修改，须在当前步骤完成并合回后再交给下一 Agent：
   - `gradle/libs.versions.toml`
   - `FCL/build.gradle.kts`
   - `FCL/src/main/AndroidManifest.xml`
   - `theme/`（FCLTheme、ColorScheme 等主题文件）
4. **可并行单元**：不同页面的 Composable 实现（各自独立的 `ui/<page>/` 目录）可并行；同一页面不拆给两个 Agent
5. **每步门禁（DoD）**：任何小步骤提交前必须通过 `./gradlew :FCL:assembleDebug` 编译，且该步骤涉及的页面人工冒烟通过；不通过则不允许进入下一步
6. **回滚策略**：每页迁移是独立 commit/分支，验收失败整页回退到 XML 版本；阶段二（Compose 基座）合入前打 tag `pre-miuix-baseline` 作为总回滚点

---

## 大计划步骤（Macro Phases）

### 阶段一：侦察与基线建立（Reconnaissance）
目标：建立迁移前的完整基线，零遗漏。**本阶段全部产物写入 `docs/migration/`。**

#### 小步骤 1.1 — UI 资产盘点
- 遍历 142 个 layout XML（按 `activity_` / `dialog_` / `item_` / 其他分类），建立清单
- 遍历 `drawable/`、`values/`（colors/themes/styles/dimens）
- 遍历 9 个 Activity、2 个 Fragment 及 `control/`、`ui/` 下的自定义 View，记录 UI 职责和生命周期依赖
- 输出：`docs/migration/ui-inventory.md`（文件路径、用途、复杂度评级 P0/P1/P2）

#### 小步骤 1.2 — 交互逻辑映射
- 分析每个 Activity/自定义 View 的事件处理（点击、长按、滑动、下拉刷新）
- 记录自定义 View 和 Adapter 的行为；记录动画；记录对话框/底部弹窗/Toast 使用场景
- 输出：`docs/migration/interaction-map.md`

#### 小步骤 1.3 — 主题与品牌资产提取
- 提取品牌主色/辅助色/强调色、字号层级、圆角/阴影/间距体系、暗黑模式方案
- 输出：`docs/migration/design-tokens.md`

#### 小步骤 1.4 — 组件映射表（新增，原方案缺失）
- 按 1.1/1.2 的清单，逐项给出"原生控件 → Miuix 组件"映射，对照 Miuix 实际源码核实组件名与能力
- 无对应组件的项（如虚拟摇杆、按键映射编辑器）标记为"保留原生 + AndroidView 包装"或"Compose 重写"，并写明理由
- 输出：`docs/migration/component-mapping.md` —— **后续阶段只许引用此表**

---

### 阶段二：架构迁移准备（Foundation）
目标：建立 Miuix 技术基座，不打无准备之仗。

#### 小步骤 2.0 — 基线快照（新增）
- 打 tag `pre-miuix-baseline`；记录当前 APK 体积、冷启动时间作为性能基线

#### 小步骤 2.1 — 依赖与 Compose 环境
- 按"已核实基线"节的写法改 `gradle/libs.versions.toml` + `FCL/build.gradle.kts`（启用 Compose 插件、`compose = true`，保留 `viewBinding`）
- 加入 Compose BOM + activity-compose + lifecycle-viewmodel-compose，版本与 Miuix 兼容矩阵核实后写入 Catalog
- 门禁：`./gradlew :FCL:assembleDebug` 通过，且现有页面（仍是 XML）运行无回归

#### 小步骤 2.2 — 主题基座
- 创建 `ui/theme/FCLTheme.kt`（封装 MiuixTheme）与 `FCLColorScheme.kt`（把 1.3 的 Design Token 映射进 Miuix ColorScheme）
- 用一个最小测试页面验证 Light/Dark 切换

#### 小步骤 2.3 — 桥接层设计
- `LegacyBridge.kt`：迁移期 Compose 界面与遗留逻辑层通信
- ViewModel 基类：统一状态管理，替代直接 View 操作
- 图片加载决策点：Glide Compose 集成 vs 新增 Coil（默认 Glide，除非有阻塞性问题）
- 输出：`docs/migration/bridge-api.md`

---

### 阶段三：核心界面逐页迁移（Page-by-Page Migration）
目标：按**风险从低到高**排序逐页迁移，每页独立分支、验收通过再合回、再做下一页。

> 原方案直接从主页开刀，风险最高——主页结构最复杂、截图对比面最大。调整为先用简单页面验证基座。

#### 小步骤 3.1 — 设置页（先行，验证基座）
- 用 `miuix-preference` 重构设置项（账户/游戏/启动器/关于分类）
- 持久化直接复用现有 DataStore 依赖
- 价值：页面结构最简单，用于验证主题、桥接层、构建链路是否合格

#### 小步骤 3.2 — 对话框体系（批量化，原方案缺失）
- 40+ 个 `dialog_*` XML 是最大批量工作：先封装统一的 `FCLDialog` 基础组件（基于映射表核实的 Miuix Dialog 组件）
- 按 1.1 的复杂度评级分批迁移，每批 ≤ 10 个

#### 小步骤 3.3 — 版本管理页
- LazyColumn 列表、滑动删除、下拉菜单、长按多选、拖拽排序
- 依赖 3.2 的对话框组件（重命名/复制版本等弹窗）

#### 小步骤 3.4 — 下载/安装流程页
- 进度指示、Dialog、下载状态管理（暂停/继续/取消）

#### 小步骤 3.5 — 账户/皮肤页
- 皮肤预览（按 2.3 决策的图片方案）、输入框、账户切换动画

#### 小步骤 3.6 — 主页（MainActivity，最后做）
- Scaffold + TopAppBar + NavigationBar 重构；版本卡片；背景壁纸 + 模糊效果（miuix-blur）
- 主页验收标准见"核心约束"节

#### 小步骤 3.7 — 其余 Activity 处置
- `WebActivity` / `ShellActivity` / `JVMActivity` / `JVMCrashActivity` / `ImportActivity`：按映射表逐个评估迁移或保留
- **`ControllerActivity`（游戏内控制界面）默认不重写**：它运行在渲染 surface 之上、直接挂钩 JNI 输入链路，属于红线区；仅做主题色对齐，不动结构

---

### 阶段四：组件级精细打磨（Component Polish）

#### 小步骤 4.1 — 自定义 View 迁移
- 按 1.4 映射表执行：可重写的写成 `@Composable`，必须保留的用 `AndroidView` 包装
- 确保触摸事件链不中断（重点：虚拟摇杆、按键映射编辑器）

#### 小步骤 4.2 — 动画体系重建
- 页面过渡 `AnimatedContent`/`Crossfade`；列表 `animateItemPlacement`；按钮按压/开关/卡片展开微交互
- 验收：动画时长与缓动曲线不劣于原版

#### 小步骤 4.3 — 暗黑模式适配
- Light / Dark / FollowSystem（Miuix 支持 Monet 时再评估）
- 所有自定义颜色提供 dark variant；验证壁纸在暗色下的对比度

---

### 阶段五：集成测试与回归验证（Integration & Regression）

#### 小步骤 5.1 — 功能对照测试
- 对照 `docs/migration/ui-inventory.md` 逐项验证：启动游戏、下载安装 Forge/Fabric/NeoForge、导入整合包/存档、微软/离线登录、虚拟鼠标与按键映射
- 输出：`docs/migration/regression-report.md`

#### 小步骤 5.2 — 性能基准测试
- 与 2.0 的基线对比：冷启动时间、内存（Profiler）、列表帧率、APK 体积
- 验收：各项指标不劣于基线（启动时间退化 ≤ 5%，内存 ≤ 10%）

#### 小步骤 5.3 — 设备兼容性测试
- minSdk 26（Android 8.0）真机/模拟器验证
- 大屏/平板（`WindowSizeClass`）、不同 DPI、刘海/挖孔安全区

---

### 阶段六：清理与文档化（Cleanup）

#### 小步骤 6.1 — 遗留代码清理
- 删除已迁移的 XML / drawable / style / 废弃 Activity，清理 Manifest 声明
- 全部迁移完成后才移除 `viewBinding` 和 `material`、`appcompat`、`constraintlayout` 依赖（提前删会破坏未迁移页面）

#### 小步骤 6.2 — 文档更新
- README 增加 Miuix 依赖说明；BUILD 文档补 Compose 编译要求
- 编写《UI 开发规范》：新增组件一律用 Miuix
- 输出：`docs/migration/final-report.md`

---

## 核心约束与红线

### 功能完整性红线
1. **游戏启动链路不可中断**：`FCLauncher` 和 `FCLCore` 的调用接口 100% 兼容
2. **文件系统操作不可变**：游戏目录、Java 运行时、渲染器插件的路径逻辑不可改动
3. **JNI/NDK 调用不可变**：PojavLauncher 后端 native 链路保持原样；`ControllerActivity` 结构不动
4. **权限体系不可变**：存储/网络权限的申请和检查逻辑保持原样

### UX/UI 迁移约束（修正版）
> 原方案"视觉还原度 ≥ 95%、像素级还原"与"迁移到 HyperOS 风格"自相矛盾——换风格必然改变像素。修正为：

1. **功能与信息层级 100% 等价**：迁移前后逐页截图对照，确认所有功能入口、状态展示、操作路径齐全；视觉风格遵循 Miuix 规范而非复刻 Material
2. **交互响应不劣于原版**：点击反馈、页面跳转、列表滚动无肉眼可感知延迟
3. **手势体系完整保留**：侧滑返回、下拉刷新、长按菜单、拖拽排序
4. **无障碍不降级**：TalkBack、字体缩放、高对比度保持支持
5. **主题定制能力保留**：用户自定义背景、配色方案继续可用

---

## 风险登记册（新增）

| 风险 | 影响 | 缓解 |
|---|---|---|
| Miuix 版本迭代快、API 不稳定 | 升级即编译错 | 锁版本；升级单独开任务并全量回归 |
| Miuix 缺少某原生控件等价物 | 交互退化 | 1.4 映射表提前暴露；AndroidView 兜底 |
| Compose 与游戏渲染 surface 共存 | 性能/内存 | ControllerActivity 不重写；Profiler 验证 |
| 多 Agent 改同一热点文件 | 合并冲突 | 协作约定第 3 条串行化 |
| 迁移期 XML/Compose 双栈长期并存 | 包体积、维护成本 | 每页独立分支快速合回；阶段六硬性清理 |

---

## 输出格式规范

每个小步骤完成后，必须输出（嵌套代码块统一用四个反引号包裹，避免破坏 Markdown 结构）：

````markdown
[步骤编号] — [步骤名称]

完成状态：✅ / ⚠️ / ❌

变更文件清单：
- `文件路径`：变更说明

关键代码片段：
```kotlin
// 核心实现代码
```

验收证据：
- 截图对比 / 测试视频 / 性能数据 / 构建结果

遗留问题：
- 如有，需明确记录并指派后续步骤
````
